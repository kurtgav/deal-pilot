{
  "name": "dealpilot-ai",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "dev:api": "pnpm --filter @dealpilot/api dev",
    "dev:web": "pnpm --filter @dealpilot/web dev"
  },
  "engines": {
    "node": ">=18"
  }
}
