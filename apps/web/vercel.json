{
  "buildCommand": "cd ../.. && pnpm --filter @dealpilot/shared build && pnpm --filter @dealpilot/web build",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
